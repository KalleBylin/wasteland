package main

// fakeWLCommonsStore has been removed — business logic tests now live in
// internal/sdk/ which has its own test infrastructure. CLI tests use SDK mocking
// via the newSDKClient package-level variable (see store_factory_test.go).
